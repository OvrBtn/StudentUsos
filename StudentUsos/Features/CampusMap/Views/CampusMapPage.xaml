<?xml version="1.0" encoding="utf-8" ?>
<controls:CustomContentPageNotAnimated
    x:Class="StudentUsos.Features.CampusMap.Views.CampusMapPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:android="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.AndroidSpecific;assembly=Microsoft.Maui.Controls"
    xmlns:controls="clr-namespace:StudentUsos.Controls"
    xmlns:strings="clr-namespace:StudentUsos.Resources.LocalizedStrings"
    xmlns:campusMap="clr-namespace:StudentUsos.Features.CampusMap.Views"
    xmlns:campusMapModels="clr-namespace:StudentUsos.Features.CampusMap.Models"
    x:DataType="campusMap:CampusMapPage"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit">

    <Grid Margin="0" RowDefinitions="auto, auto, *">

        <CollectionView Grid.Row="0" ItemsSource="{Binding Buildings}" HorizontalOptions="Center" Margin="0, 30, 0, 0">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Horizontal"></LinearItemsLayout>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="campusMapModels:CampusBuilding">
                    <Button Text="{Binding Id}" Margin="5, 0" BackgroundColor="{StaticResource Primary}"></Button>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Floors}" HorizontalOptions="Center" Margin="0,15,0,15">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Horizontal"></LinearItemsLayout>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="{x:Type x:String}">
                    <Button Text="{Binding .}" Margin="5, 0" BackgroundColor="{StaticResource Primary}"></Button>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Grid Grid.Row="2">

            <ActivityIndicator/>
            <HybridWebView x:Name="hybridWebView" RawMessageReceived="OnHybridWebViewRawMessageReceived" Background="Transparent"/>

        </Grid>

        <VerticalStackLayout Grid.RowSpan="3" VerticalOptions="End" HorizontalOptions="Fill" 
                             InputTransparent="True" CascadeInputTransparent="False">
            <Border HorizontalOptions="End" Style="{StaticResource BorderWithShadow}" Padding="0"
                    BackgroundColor="{StaticResource Primary}" Margin="40, 80" Stroke="Transparent">
                <Grid ColumnDefinitions="auto, auto">
                    <Button Grid.Column="0" BackgroundColor="Transparent" Text="-" 
                            FontSize="20" Padding="15, 10, 10, 10" CornerRadius="0"/>
                    <Button Grid.Column="1" BackgroundColor="Transparent" Text="+" 
                            FontSize="20" Padding="10, 10, 15, 10" CornerRadius="0"/>
                </Grid>
            </Border>
        </VerticalStackLayout>

    </Grid>

</controls:CustomContentPageNotAnimated>
